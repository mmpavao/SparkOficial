# Database Configuration
# Para Docker (padrão)
DATABASE_URL=postgresql://spark_user:spark123456@localhost:5432/spark_comex

# Para PostgreSQL local (se não usar Docker)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/spark_comex

# Environment
NODE_ENV=development

# Server Configuration
PORT=5000

# Docker Configuration
DOCKER_POSTGRES_HOST=localhost
DOCKER_POSTGRES_PORT=5432
DOCKER_POSTGRES_USER=spark_user
DOCKER_POSTGRES_PASSWORD=spark123456
DOCKER_POSTGRES_DB=spark_comex

# Optional: External Services (configure se necessário)
# STRIPE_SECRET_KEY=sk_test_...
# GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
